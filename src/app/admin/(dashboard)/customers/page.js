import Customers from '../../containers/Customers';

export const metadata = {
  title: 'Customers',
  description: '...',
};

const CustomersPage = () => {
  return <Customers />;
};

export default CustomersPage;
