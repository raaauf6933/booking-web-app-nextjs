import Bookings from '../../containers/Bookings';

export const metadata = {
  title: 'Bookings',
  description: '...',
};

const BookingsPage = () => {
  return <Bookings />;
};

export default BookingsPage;
